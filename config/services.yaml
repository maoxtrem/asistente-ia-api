# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.assistant_name: '%env(string:ASSISTANT_NAME)%'
    app.qdrant_url: '%env(string:QDRANT_URL)%'
    app.qdrant_collection: '%env(string:QDRANT_COLLECTION)%'
    app.ollama_url: '%env(string:OLLAMA_URL)%'
    app.ollama_embedding_model: '%env(string:OLLAMA_EMBEDDING_MODEL)%'
    app.ollama_chat_model: '%env(string:OLLAMA_CHAT_MODEL)%'
    app.ollama_chat_timeout: '%env(float:OLLAMA_CHAT_TIMEOUT)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\Service\QdrantClient:
        arguments:
            $qdrantUrl: '%app.qdrant_url%'

    App\Service\EmbeddingClient:
        arguments:
            $ollamaUrl: '%app.ollama_url%'
            $embeddingModel: '%app.ollama_embedding_model%'

    App\Service\VectorContextRetriever:
        arguments:
            $qdrantCollection: '%app.qdrant_collection%'

    App\Service\OllamaChatClient:
        arguments:
            $ollamaUrl: '%app.ollama_url%'
            $chatModel: '%app.ollama_chat_model%'
            $timeout: '%app.ollama_chat_timeout%'

    App\Contract\ChatProviderInterface: '@App\Service\OllamaChatClient'

    App\Service\IndexDocumentProcessor:
        arguments:
            $qdrantCollection: '%app.qdrant_collection%'

    App\Controller\Api\ChatController:
        arguments:
            $assistantName: '%app.assistant_name%'

    App\Controller\Api\HealthController:
        arguments:
            $assistantName: '%app.assistant_name%'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
